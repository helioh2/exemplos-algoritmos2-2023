/**
 * Calcule a potenciação de forma recursiva
*/

#include <assert.h>

//prototipo:
int pot(int b, int e) {
    return 0;
}

int main() {

    assert(pot(2,0) == 1);
    assert(pot(2,1) == 2);
    assert(pot(2,2) == 4);
    assert(pot(2,3) == 8);

    return -1;
}